# Bootstrap only — values migrate into runtime/data.db on first run
GPT_REG_ROOT=
RUNTIME_DIR=runtime
WEB_PORT=2023

# Optional seed (multiline proxy.pool can also be set via Settings UI)
# PROXY_POOL=zhanghao001-region-VN:pass@host:3010
